#include "main.h"

#include <stdio.h>

/**
 * _isdigit - Write a function that checks for a digit (0 through 9)
 *
 * @c : the prototype
 *
 * Return : 1 if c is a digit
 *
 * 0 if  otherwise
 */

int _isdigit(int c)
{
	char a;
	int isdigit = '0';

	for (a = '0'; a <= '9'; a++)
	{
		if (a == 0)
		return (0);
	}
}

